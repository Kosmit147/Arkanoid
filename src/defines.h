#pragma once

// (0, 0) is at the bottom-left corner
#define COORDINATE_SPACE 4096
#define COORDINATE_SPACE_SCALING (COORDINATE_SPACE / 1000.0f)

#define PADDLE_WIDTH (150.0f * COORDINATE_SPACE_SCALING)
#define PADDLE_HEIGHT (50.0f * COORDINATE_SPACE_SCALING)
#define PADDLE_START_POS_X ((float)COORDINATE_SPACE / 2 - PADDLE_WIDTH / 2)
#define PADDLE_START_POS_Y ((float)COORDINATE_SPACE / 6)
#define PADDLE_SPEED (500.0f * COORDINATE_SPACE_SCALING)

#define BLOCK_VERTEX_FLOATS 2